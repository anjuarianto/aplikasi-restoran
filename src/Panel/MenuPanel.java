package Panel;

import javax.swing.*;

public class MenuPanel extends JPanel {

    public MenuPanel(JPanel panel) {
        JLabel label = new JLabel();
        label.setText("Ini contoh panel lain, berhasil pindah");
        add(label);
    }
}
